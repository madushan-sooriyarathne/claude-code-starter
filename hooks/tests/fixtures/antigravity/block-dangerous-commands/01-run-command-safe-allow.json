{
  "name": "safe run_command passes through as allow",
  "stdin": { "toolCall": { "name": "run_command", "args": { "CommandLine": "ls -la" } } },
  "expect_exit": 0,
  "expect_stdout_contains": ["\"decision\":\"allow\""]
}
