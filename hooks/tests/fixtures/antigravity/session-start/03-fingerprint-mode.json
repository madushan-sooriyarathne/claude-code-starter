{
  "name": "fingerprint mode prints manifest hash JSON, bypassing the hook contract",
  "stdin": {},
  "env": { "AGENT_STARTER_FINGERPRINT": "1" },
  "expect_exit": 0,
  "expect_stdout_contains": ["manifest_hash", "setup_date"]
}
