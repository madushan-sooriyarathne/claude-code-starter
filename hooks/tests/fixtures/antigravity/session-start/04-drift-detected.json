{
  "name": "stale fingerprint triggers a config-drift ephemeralMessage (run from repo root)",
  "stdin": { "invocationNum": 0, "initialNumSteps": 0, "conversationId": "ag-session-4" },
  "env": { "AGENT_STARTER_META": "hooks/tests/fixtures/antigravity/session-start/data/meta-stale.json" },
  "expect_exit": 0,
  "expect_stdout_contains": ["config drift", "/setup-agents"]
}
