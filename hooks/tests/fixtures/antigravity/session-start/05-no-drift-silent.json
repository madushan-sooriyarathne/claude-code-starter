{
  "name": "matching fingerprint stays silent on drift (hash = empty-manifest cksum of this repo)",
  "stdin": { "invocationNum": 0, "initialNumSteps": 0, "conversationId": "ag-session-5" },
  "env": { "AGENT_STARTER_META": "hooks/tests/fixtures/antigravity/session-start/data/meta-match.json" },
  "expect_exit": 0,
  "expect_stdout_not_contains": ["config drift"]
}
