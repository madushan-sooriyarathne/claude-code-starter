{
  "name": "stale fingerprint triggers one-line drift nudge (run from repo root)",
  "stdin": {},
  "env": { "CLAUDE_CODE_STARTER_META": "hooks/tests/fixtures/session-start/data/meta-stale.json" },
  "expect_exit": 0,
  "expect_stdout_contains": ["config drift", "/setup-claude"]
}
